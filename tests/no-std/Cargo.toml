[package]
name = "no-std"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"

[dependencies]
futures-async-stream = { path = "../.." }
futures-core = { version = "0.3", default-features = false }
