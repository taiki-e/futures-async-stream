[package]
name = "no-std"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
futures-async-stream = { path = "../.." }
futures-core = { version = "0.3", default-features = false }

[lints]
workspace = true
